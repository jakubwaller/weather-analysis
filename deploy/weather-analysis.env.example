# Copy to deploy/weather-analysis.env (git-ignored) and fill in.
# Home Assistant -> your profile -> Security -> Long-lived access tokens.
HA_TOKEN=

# Login for the public dashboard (auth sidecar behind Caddy forward_auth).
# COOKIE_SECRET: python -c 'import secrets;print(secrets.token_urlsafe(32))'
WEB_PASSWORD=
COOKIE_SECRET=
